# Contracts

Language-agnostic shapes for REST, WebSocket, and Redis Stream events.

- `openapi.yaml` — HTTP API
- `events/` — JSON Schema for stream/WS payloads

Generate Go/TS types in a later PR. Do not hand-duplicate event names across `apps/web` and `apps/api` once this is filled in.
