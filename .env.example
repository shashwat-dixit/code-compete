# Copy to .env (gitignored). Never put real secrets in this file.

API_PORT=8080
DATABASE_URL=postgres://codecompete:codecompete@localhost:5432/codecompete?sslmode=disable
REDIS_URL=redis://localhost:6379/0

# Filled in the auth PR. Leave empty until then.
OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=
OAUTH_GITHUB_REDIRECT_URL=http://localhost:8080/auth/github/callback
SESSION_SECRET=change-me-in-dev-only
