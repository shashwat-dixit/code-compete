# Vite + React frontend for Code Compete.

```bash
bun install
bun run dev
```

Routes (stubs): `/`, `/login`, `/compete`, `/rankings`, `/match/:matchId`.

The Vite dev server proxies `/healthz` and `/api` to `http://localhost:8080`.
